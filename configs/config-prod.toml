[env]
# Secrets are injected via environment variables (MAL_CLIENT_ID, ANILIST_CLIENT_ID).
# Do NOT use ${...} placeholders here — koanf does not expand them.
malClientId = ""
anilistClientId = ""
appEnv = "prod"

[logger]
logLevel = "info"
pretty = false
